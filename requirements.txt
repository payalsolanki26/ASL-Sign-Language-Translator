# --- core (data engineering, security, visualization: verified in development) ---
numpy>=1.26
pandas>=2.0
Pillow>=10.0
opencv-python-headless>=4.8
scikit-learn>=1.3
matplotlib>=3.8
seaborn>=0.13
PyYAML>=6.0
cryptography>=42.0

# --- deep learning (needs a machine with PyTorch; GPU strongly recommended) ---
torch>=2.2
torchvision>=0.17

# --- serving / live inference (optional) ---
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
mediapipe>=0.10        # live hand-landmark overlay (Tasks API)
onnx>=1.16             # optional export
